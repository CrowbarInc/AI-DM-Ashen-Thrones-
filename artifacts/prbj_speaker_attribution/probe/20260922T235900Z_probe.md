# PR-BJ speaker-attribution probe

Source JSON: `artifacts/prbj_speaker_attribution/probe/20260922T235900Z_probe.json`

Historical recovered turn: PR-AH extended replay T11, `artifacts/prah_grounded_observation/extended_replay/runs/20260920T020729Z_R2-MT01-AH/transcript.md`.

## Current recovered turn

Player: `I look for signs of the patrol.`

Scene: `old_milestone`. Present NPCs: none (gate NPCs remain at `frontier_gate`).

| Model text | Kind | Speaker | Final narration |
| --- | --- | --- | --- |
| Neutral search line | `investigate` | none | Visible milestone stock. No `"The guard says"`. |
| `The guard says, "I do not know enough to answer that."` | `investigate` | none | Same visible milestone stock. The model line is replaced. |

## Live identity matrix

| Case | Resolved speaker | Final label |
| --- | --- | --- |
| Explicit Gate Serjeant | `gate_serjeant` | `Gate Serjeant mutters, "Word is, ..."` |
| Explicit Guard Captain, model uses `"The guard says"` | `guard_captain` | `Guard Captain mutters, "Word is, ..."` |
| Sole Gate Guard, undirected | `gate_guard` | `Gate Guard mutters, "Word is, ..."` |
| Sole Tavern Runner, model uses `"The guard says"` | `tavern_runner` | Quoted ignorance only. Name not in the final line. `"The guard"` is not kept. |
| Ward Sentry, internal id `secret_warden` | `secret_warden` | `Ward Sentry mutters, ...` Internal id is not shown. |
| `"Guard, who commands the watch here?"` among several NPCs | `guard_captain` | `Guard Captain mutters, ...` |
| `"What's nearby from here?"` with several NPCs present | none | `The murmur around you never tightens into a single clear voice on that point.` |
| `"Who commands the watch here?"` at `old_milestone`, no NPC present | none | Milestone observation stock. |
| `"I look around the gate."` | none | Visible gate stock. No NPC speech. |

## Helper contract when identity fields are empty

`speaker_label(None)`, `speaker_label({})`, and `speaker_label({"social": {}})` return `The guard`.

The emergency and ownership-terminal catalogs then emit:

`The guard says, "I do not know enough to answer that."`

Named resolutions return the authored name (`Gate Serjeant`, `Guard Captain`, `Tavern Runner`, `Gate Guard`). An id with no name is title-cased (`secret_warden` -> `Secret Warden`). Empty `npc_display_name_for_emission` also returns `The guard`.

That helper string did not reach the player on any chat row in this probe.
